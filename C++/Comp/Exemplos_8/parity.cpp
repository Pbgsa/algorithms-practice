#include <bits/stdc++.h>
using namespace std;

#define pb push_back
#define all(v) v.begin(), v.end()
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // While n != 0, read n and then n integers, outputting their parity
    int n;
    while (cin >> n, n != 0) {
        // Print n until a zero is encountered
        for (int i = 0; i < n; i++) {
            int x;
    }

    return 0;
}